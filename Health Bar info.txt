Health Bar
using UnityEngine.UI
class Image
var FillAmount (0-1)
if(FillAmount > 0) {
	health -= powerLevel;
}


PowerUp Class
float powerLevel


enemy
to go back and forth
set the transform to an object until it hits the object then changes the transform to another object

turn off mesh renderer to make transparent